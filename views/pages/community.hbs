<h1>Community</h1>

<div class="container">
  <div class="row align-items-center justify-content-around">
<div class="col-sm">
        {{#each community}}
    <h4>{{username}}</h4>
    <img style="width: 4rem;"src="{{imageUrl}}">
    {{/each}}
</div>
  </div>
</div>

